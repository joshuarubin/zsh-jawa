#!/usr/bin/env zsh

if [ -n "${WEZTERM_PANE}" ]; then
  if [ -n "${XDG_RUNTIME_DIR}" ]; then
    echo "--listen ${XDG_RUNTIME_DIR}/nvim-wezterm-pane-${WEZTERM_PANE}"
  elif [ -n "${TMPDIR}" ]; then
    echo "--listen ${TMPDIR}/nvim-wezterm-pane-${WEZTERM_PANE}"
  fi
fi
